# 速度课程设计

## 1. 适用范围

本文档对应当前 Go2 平地速度训练任务：

~~~text
Mjlab-Velocity-Flat-Unitree-Go2
~~~

主要实现文件：

~~~text
src/mjlab/tasks/velocity/mdp/curriculums.py
src/mjlab/tasks/velocity/mdp/velocity_command.py
src/mjlab/tasks/velocity/config/go2/env_cfgs.py
~~~

这套课程主要解决两个问题：

1. 逐环境速度 cap 导致 command 分布持续变化，进而引起 reward、policy std 和 critic value loss 波动；
2. 机器人从 1 m/s 切换到 2 m/s 可以运动，但 reset 后直接给 2 m/s 不会启动。

---

## 2. 核心思路

当前使用全局共享的速度阶段，不再让每个并行环境独立升速或降速。

每个速度阶段包含两个 phase：

~~~text
phase 0：当前速度范围 + 低随机化
    ↓ 指标达标
phase 1：当前速度范围 + 当前阶段随机化
    ↓ 指标再次达标
进入下一个速度阶段，并回到 phase 0
~~~

完整训练流程为：

~~~text
S0/phase 0 → S0/phase 1
S1/phase 0 → S1/phase 1
S2/phase 0 → S2/phase 1
~~~

设计原则是：

> 先学会当前速度，再学习当前速度下的抗扰动能力，最后提升速度。

进入新速度阶段时，随机化会暂时降低，避免速度难度和扰动难度同时突然增加。

---

## 3. 速度阶段

| 阶段 | x 速度范围 | y 速度范围 | yaw 速度范围 | phase 1 随机化强度 |
|---|---:|---:|---:|---:|
| S0 | [-1.0, 1.0] | [-0.3, 0.3] | [-0.5, 0.5] | 0.35 |
| S1 | [-1.0, 1.5] | [-0.4, 0.4] | [-0.6, 0.6] | 0.60 |
| S2 | [-1.0, 2.0] | [-0.5, 0.5] | [-0.7, 0.7] | 1.00 |

### S0

S0 训练站立、从静止启动、0.5 m/s 和 1.0 m/s 速度跟踪。此时不要求 2 m/s 直接启动，因为 2 m/s 命令尚未开放。

### S1

S1 加入 1.5 m/s 速度，同时适度增加侧向和转向范围。进入 S1 后重新从低随机化 phase 0 开始。

### S2

S2 开放 2.0 m/s，并且增加两个额外门槛：

- 高速跟踪质量；
- reset 后直接高速启动成功率。

S2/phase 1 达标后保持在最终阶段，不再继续提升速度。

---

## 4. 固定速度桶

当前 x 速度使用固定桶采样，而不是在连续区间内均匀采样：

| 速度桶 | 采样权重 |
|---:|---:|
| [-1.0, -0.5] | 0.10 |
| 0.0 | 0.15 |
| 0.5 | 0.20 |
| 1.0 | 0.25 |
| 1.5 | 0.15 |
| 2.0 | 0.15 |

只会采样当前阶段已经开放的桶：

~~~text
S0：-1~-0.5、0、0.5、1.0
S1：-1~-0.5、0、0.5、1.0、1.5
S2：-1~-0.5、0、0.5、1.0、1.5、2.0
~~~

权重会在可用桶内重新归一化。

固定桶的好处：

- 0.5、1.0、1.5、2.0 m/s 都有明确样本；
- 可以保证 2 m/s 样本数量；
- 可以单独统计高速跟踪；
- 可以训练 reset 后直接给高速命令；
- 不会因为连续均匀采样而几乎采不到精确高速指令。

rel_standing_envs = 0.15 表示约 15% 的环境会被分配为零速度站立环境。由于速度桶本身也含 0 m/s，实际站立样本比例可能更高。

命令重采样时间为：

~~~text
resampling_time_range = (5.0, 5.0)
~~~

也就是每 5 秒重新采样一次命令。

---

## 5. 高速直接启动

### 5.1 尝试的识别

一次 reset 后，如果同时满足：

~~~text
|command_x| >= 1.8 m/s
初始 xy 速度 <= 0.1 m/s
~~~

则该 episode 记为一次高速直接启动尝试。

这能区分已有运动惯性后从 1 m/s 切换到 2 m/s，和机器人静止 reset 后直接收到 2 m/s。

### 5.2 单次尝试成功标准

直接启动后观察 2 秒：

~~~text
速度误差 <= 0.35 m/s 的时间步比例 >= 70%
~~~

参数含义：

| 参数 | 当前值 | 作用 |
|---|---:|---|
| direct_start_speed_threshold | 1.8 | 进入高速直接启动统计的命令阈值。 |
| direct_start_window_s | 2.0 | 启动后的观察时间。 |
| direct_start_tracking_tolerance | 0.35 | 单步速度误差允许值。 |
| direct_start_success_ratio | 0.7 | 单次尝试内合格时间步的最低比例。 |

### 5.3 阶段门槛

整个评估窗口内，直接启动成功率需要达到：

~~~text
direct_start_success >= 0.8
~~~

这里的 0.8 是 episode 级别成功率，和单次尝试内部使用的 0.7 不同。

---

## 6. 阶段切换条件

### 6.1 episode 计数

episode 计数是所有并行环境完成的 episode 总数。

当前参数：

~~~text
min_phase_episodes = 16384
evaluation_window_episodes = 8192
required_windows = 2
~~~

含义：

- 每个 phase 至少训练 16384 个完成的 episode；
- 每 8192 个 episode 计算一次指标；
- 连续两个窗口达标后才切换；
- 某个窗口不达标时，连续达标计数清零，但不会自动降回上一阶段。

如果使用 4096 个并行环境：

~~~text
16384 / 4096 ≈ 4 个完整 episode 等效时长
8192 / 4096 ≈ 2 个完整 episode 等效时长
~~~

降低并行环境数后，达到同样 episode 数需要更长的真实训练时间。

### 6.2 基础达标条件

S0 和 S1 使用：

~~~text
survival_rate >= 0.95
track_quality >= 0.75
~~~

### 6.3 S2 额外条件

S2 还要求：

~~~text
high_speed_track_quality >= 0.65
direct_start_success >= 0.80
~~~

### 6.4 状态转换

~~~text
phase 0 达标
    → 当前 stage 的 phase 1

phase 1 达标
    → 下一个 stage 的 phase 0
~~~

---

## 7. 指标计算

### 7.1 x 速度跟踪质量

每个时间步计算：

~~~text
e_x = |command_x - actual_x|
q_x = exp(-(e_x / track_x_quality_std)^2)
~~~

当前 track_x_quality_std = 0.5。

大致对应关系：

| 速度误差 | 质量分数 |
|---:|---:|
| 0.00 | 1.00 |
| 0.25 | 0.78 |
| 0.35 | 0.61 |
| 0.50 | 0.37 |

track_quality 是 episode 内 q_x 的平均值，再对评估窗口内的 episode 求平均。

### 7.2 存活率

如果：

~~~text
episode_length >= 0.95 × max_episode_length
~~~

就认为该 episode 存活。

survival_rate 是评估窗口内存活 episode 的比例。

### 7.3 高速跟踪质量

只有：

~~~text
|command_x| >= 1.8 m/s
~~~

的时间步会进入 high_speed_track_quality。这样低速和站立样本不会掩盖高速跟踪失败。

参数：

~~~text
high_speed_metric_threshold = 1.8
high_speed_track_threshold = 0.65
~~~

---

## 8. 随机化阶段

### 8.1 随机推力

训练中的 push_robot 事件仍使用原来的随机时间间隔：

~~~text
interval_range_s = (1.0, 5.0)
~~~

但推力范围乘以当前 phase 的随机化强度：

| 状态 | 推力强度 |
|---|---:|
| 任意 stage / phase 0 | 0% |
| S0 / phase 1 | 35% |
| S1 / phase 1 | 60% |
| S2 / phase 1 | 100% |

phase 0 的推力强度为 0 时，事件函数直接跳过。

### 8.2 质量随机化

当前课程控制两类质量相关随机化：

1. link 的物理一致质量随机化；
2. base 的额外质量偏移。

这两个事件改成 reset 事件：

- phase 0：每次 reset 恢复标称质量；
- phase 1：按当前 stage 的强度采样质量；
- 进入新 stage 后，下一次 reset 先恢复到标称质量。

### 8.3 尚未纳入阶段缩放的随机化

以下随机化仍然保留在 startup 阶段：

- 脚底摩擦；
- 自旋摩擦和滚动摩擦；
- encoder bias；
- base COM 偏移；
- joint damping；
- PD gains；
- effort limits。

因此当前“phase 0 关闭随机化”的准确含义是：

~~~text
关闭课程控制的随机推力和质量/载荷随机化
~~~

并不是完全关闭所有 domain randomization。

如果后续发现基础学习仍然受到摩擦、PD 或 COM 随机化影响，再考虑把这些项目也改成阶段化 reset 事件。

---

## 9. 参数完整说明

### 9.1 命令参数

| 参数 | 当前值 | 作用 |
|---|---:|---|
| resampling_time_range | (5.0, 5.0) | 命令每 5 秒采样一次。 |
| velocity_buckets | 见第 4 节 | x 速度桶和权重。 |
| rel_standing_envs | 0.15 | 零速度站立环境比例。 |
| track_x_quality_std | 0.5 | 速度误差质量函数的尺度。 |
| high_speed_metric_threshold | 1.8 | 高速统计的命令阈值。 |
| direct_start_speed_threshold | 1.8 | 高速直接启动的命令阈值。 |
| direct_start_window_s | 2.0 | 高速启动观察时间。 |
| direct_start_tracking_tolerance | 0.35 | 高速启动允许的单步误差。 |
| direct_start_success_ratio | 0.7 | 单次启动的时间步成功比例。 |

### 9.2 课程参数

| 参数 | 当前值 | 作用 |
|---|---:|---|
| stages | S0/S1/S2 | 各阶段速度范围和随机化强度。 |
| min_phase_episodes | 16384 | 一个 phase 的最小 episode 数。 |
| evaluation_window_episodes | 8192 | 一个评估窗口的 episode 数。 |
| required_windows | 2 | 连续达标窗口数。 |
| survival_threshold | 0.95 | 存活率门槛。 |
| track_threshold | 0.75 | 全部 x 速度跟踪质量门槛。 |
| high_speed_track_threshold | 0.65 | S2 高速跟踪质量门槛。 |
| direct_start_threshold | 1.8 | 触发高速直接启动要求的速度上限。 |
| direct_start_success_threshold | 0.8 | 高速直接启动 episode 成功率门槛。 |

randomization_scale 不是学习率，也不是 reward 权重，只缩放课程控制的推力和质量随机化范围。

---

## 10. 日志和观察重点

主要课程日志：

~~~text
Curriculum/command_vel/stage
Curriculum/command_vel/phase
Curriculum/command_vel/randomization_level
Curriculum/command_vel/survival_rate
Curriculum/command_vel/track_quality
Curriculum/command_vel/high_speed_track_quality
Curriculum/command_vel/direct_start_success
~~~

| 日志 | 含义 |
|---|---|
| stage | 0、1、2 对应 S0、S1、S2。 |
| phase | 0 是低随机化，1 是当前等级随机化。 |
| randomization_level | 当前推力和质量随机化强度。 |
| survival_rate | 最近评估窗口的存活率。 |
| track_quality | 所有 x 速度样本的跟踪质量。 |
| high_speed_track_quality | 1.8 m/s 以上样本的跟踪质量。 |
| direct_start_success | reset 后高速直接启动的成功率。 |

建议同时观察：

- reward；
- value loss；
- policy std；
- episode length；
- stage、phase 和 randomization_level。

正常推进时：

~~~text
stage 提升时，速度范围增加；
phase 从 0 变为 1 时，randomization_level 增加；
进入新 stage 后，phase 回到 0。
~~~

---

## 11. 训练和播放

### 11.1 新开训练

在项目根目录执行：

~~~bash
uv run train Mjlab-Velocity-Flat-Unitree-Go2 \
  --num-envs 4096 \
  --agent.max-iterations 100000
~~~

显存不足时可以使用 2048 或更少的并行环境：

~~~bash
uv run train Mjlab-Velocity-Flat-Unitree-Go2 \
  --num-envs 2048 \
  --agent.max-iterations 100000
~~~

降低 num-envs 后，达到同样的 min_phase_episodes 需要更长的真实训练时间。

### 11.2 TensorBoard

~~~bash
uv run tensorboard --logdir logs/rsl_rl
~~~

### 11.3 播放 checkpoint

~~~bash
uv run play Mjlab-Velocity-Flat-Unitree-Go2 \
  --checkpoint-file \
  logs/rsl_rl/go2_flat/<run-time>/model_<iteration>.pt
~~~

play 模式：

- 使用 1 个环境；
- 速度由网页滑块控制；
- 不执行速度桶课程；
- 不执行训练阶段切换；
- 保留随机推力，用于检查抗扰动能力。

建议按以下顺序测试：

1. reset 后给 0.5 m/s；
2. reset 后给 1.0 m/s；
3. reset 后给 1.5 m/s；
4. reset 后直接给 2.0 m/s；
5. 从 1.0 m/s 切换到 2.0 m/s；
6. 观察受到推力后能否恢复。

重点比较第 4 项和第 5 项。

---

## 12. checkpoint 和恢复训练

当前 stage、phase、episode 计数和评估窗口状态保存在运行进程中，没有作为独立课程状态保存到 RSL-RL checkpoint。

因此改变课程结构后，推荐新开训练，不建议直接从旧 run 的后期 checkpoint 恢复。

特别不要从已经出现 value loss 为 inf/nan 的 checkpoint 继续。

如果必须利用旧模型，建议：

- 只使用早期且有限值正常的 checkpoint；
- 优先加载 actor；
- 重置 critic；
- 重置 optimizer；
- 重新从 S0/phase 0 开始课程。

---

## 13. 常见问题

### 长时间停在 S0/phase 0

检查：

- survival_rate 是否低于 0.95；
- track_quality 是否低于 0.75；
- 0.5 和 1.0 m/s 是否能稳定启动；
- episode length 是否接近完整 episode；
- 基础摩擦、PD 或动作延迟随机化是否过强。

### 进入 phase 1 后性能下降

说明低随机化下已经会走，但抗扰动能力不足。先观察当前 phase，不要马上提高速度。

### S2/phase 0 无法达标

重点检查：

- high_speed_track_quality 是否低于 0.65；
- direct_start_success 是否低于 0.8；
- 2 m/s 桶是否实际采样；
- reset 后直接给 2 m/s 是否失败。

### 总 reward 正常但高速启动失败

总 reward 可能被低速和站立样本抬高。此时应优先看：

~~~text
high_speed_track_quality
direct_start_success
~~~

不要只看总 reward。

### value loss 再次出现 inf/nan

立即停止该 run，检查：

1. NaN 首次出现的 iteration；
2. 是否刚发生 stage 或 phase 切换；
3. 是否出现异常大的 episode reward；
4. survival_rate 是否突然下降；
5. randomization_level 是否突然升高；
6. critic/value loss 是否先于 actor 和 reward 崩溃。

不要从 NaN 之后的 checkpoint 继续。

---

## 14. 调参建议

如果课程推进太慢，可以依次尝试：

1. min_phase_episodes：16384 → 8192；
2. required_windows：2 → 1；
3. track_threshold：0.75 → 0.70；
4. survival_threshold：0.95 → 0.90。

如果推进太快或高速不稳定，可以依次尝试：

1. required_windows：2 → 3；
2. min_phase_episodes：16384 → 32768；
3. high_speed_track_threshold：0.65 → 0.70；
4. direct_start_success_threshold：0.80 → 0.85；
5. 降低 S0/S1 的 randomization_scale；
6. 延后开放 1.5 m/s 和 2.0 m/s 速度桶。

不建议恢复逐环境 cap，也不建议用旧课程的 decay 或 step_up 解决当前阶段切换问题。

---

## 15. 设计总结

当前设计由以下部分组成：

~~~text
固定速度桶
    +
全局速度阶段
    +
每阶段先低随机化、后高随机化
    +
2 m/s reset 直接启动专项统计
    +
连续评估窗口达标后切换
~~~

最终要验证的不是“平均 reward 是否够高”，而是机器人是否同时具备：

- 从静止启动；
- 稳定跟踪 0.5/1.0/1.5/2.0 m/s；
- 速度切换；
- 受到随机推力后恢复；
- 在质量变化下保持稳定；
- 不依赖已有运动惯性才能启动。
