# 日志参数详解

本文对应本次平地速度训练日志：

`/home/haozi/桌面/mjlab/logs/rsl_rl/go2_flat/2026-08-15_09-09-24/`

文档分为两部分：第一部分解释本次日志中会影响训练和判断结果的参数；第二部分给出一套可以直接照着 TensorBoard 判断速度追踪效果、课程阶段和是否继续训练的方法。

> 说明：日志中的 YAML 是运行时配置的序列化结果，因此包含大量 `asset_cfg`、对象类型和默认字段。本文逐项解释训练控制、速度课程、随机化、奖励、PPO 和诊断指标；重复出现的对象筛选字段（例如 `joint_ids: slice(None)`）不再逐项重复解释。

## 1. 本次 run 的基本信息

| 参数 | 实际值 | 含义 |
|---|---:|---|
| `seed` | `42` | 随机种子。固定后便于复现实验，但不同硬件或并行执行仍可能有细微差异。 |
| `scene.num_envs` | `4096` | 并行环境数量。数量越大，每次迭代获得的样本越多，但显存占用也越高。 |
| `terrain.terrain_type` | `plane` | 平地任务，不使用复杂地形课程。 |
| `episode_length_s` | `20.0` | 单个 episode 的最长仿真时间。超时属于正常结束，不属于摔倒。 |
| `save_interval` | `200` | 每隔 200 个 PPO iteration 保存 checkpoint。 |
| `max_iterations` | `15000` | 计划最多训练的 PPO iteration 数。不是 episode 数。 |
| `experiment_name` | `go2_flat` | 实验名称。 |
| `resume` | `false` | 本次 run 不是从 checkpoint 恢复启动的配置。 |

### 1.1 时间尺度

本次配置为：

```text
Mujoco timestep = 0.005 s
decimation       = 4
RL step          = 0.005 × 4 = 0.020 s
episode          = 20 / 0.020 ≈ 1000 个 RL step
PPO rollout      = 24 个 RL step / 环境 / iteration
```

因此：

- `decimation` 表示每个策略动作保持多少个 MuJoCo 子步，不是 PPO 的步数。
- `num_steps_per_env=24` 表示每个环境每个 PPO iteration 收集 24 个策略步。
- 如果所有环境都完整跑完一个 20 秒 episode，4096 个环境大约产生 4096 个 episode 结束事件；所以课程中的 episode 计数是并行环境累计数，不能把 TensorBoard 横轴直接当成 episode。
- 本次日志有 378 个 TensorBoard 标量点，训练仍处于非常早期，不能据此判断最终高速能力。

## 2. 仿真与场景参数

### 2.1 仿真器参数

| 参数 | 实际值 | 作用 |
|---|---:|---|
| `sim.mujoco.timestep` | `0.005` | MuJoCo 物理积分步长，单位秒。越小通常越稳定，但计算量更大。 |
| `decimation` | `4` | 一个策略动作对应 4 个物理步。 |
| `sim.mujoco.integrator` | `implicitfast` | 积分器。对刚性接触和快速运动通常比显式积分更稳。 |
| `sim.mujoco.solver` | `newton` | 接触约束求解器。 |
| `sim.mujoco.iterations` | `10` | 每个物理步的求解迭代次数。 |
| `sim.mujoco.tolerance` | `1e-8` | 求解收敛容差。 |
| `sim.mujoco.impratio` | `10` | 阻尼/阻抗相关的求解参数。 |
| `sim.mujoco.gravity` | `(0, 0, -9.81)` | 重力加速度，单位 m/s²。 |
| `sim.njmax` | `300` | MuJoCo 关节/约束相关容量上限。 |
| `sim.contact_sensor_maxmatch` | `64` | 接触传感器允许匹配的最大接触数。 |

### 2.2 NaN 防护

```yaml
sim:
  nan_guard:
    enabled: false
    buffer_size: 100
    output_dir: /tmp/mjlab/nan_dumps
    max_envs_to_dump: 5
```

- `nan_guard.enabled=false`：本次没有开启运行时 NaN 快照保护。训练出现 NaN 时，日志中可能只能看到后续 loss/reward 异常，无法自动保存出问题环境的状态。
- `buffer_size=100`：如果打开，通常表示保留异常前的最近若干步状态。
- `output_dir`：异常快照输出目录。
- `max_envs_to_dump=5`：最多保存多少个异常环境。

这几个参数只影响异常诊断，不会改变正常策略的奖励或课程逻辑。

## 3. 观测、历史和动作

### 3.1 Actor 观测

Actor 使用本体感觉和命令：

| 观测项 | 内容 | 本次处理 |
|---|---|---|
| `base_ang_vel` | 机体角速度 | 加性噪声 `[-0.2, 0.2]`，随机延迟 1--3 步，延迟保持概率 `0.9`。 |
| `projected_gravity` | 重力方向在机体坐标系的投影 | 加性噪声 `[-0.05, 0.05]`。 |
| `joint_pos` | 关节相对位置 | 加性噪声 `[-0.01, 0.01]`，随机延迟 1--3 步，保持概率 `0.9`。 |
| `joint_vel` | 关节相对速度 | 加性噪声 `[-1.5, 1.5]`，随机延迟 1--3 步，保持概率 `0.9`。 |
| `actions` | 上一个动作 | 不加噪声。 |
| `command` | 当前速度命令 `twist` | 不加噪声。 |

Actor 相关参数：

- `enable_corruption=true`：启用观测噪声和延迟等观测扰动。
- `history_length=10`：策略看到最近 10 帧历史，有助于从时间序列中估计速度、延迟和接触状态。
- `history_ordering=time`：按时间顺序排列历史。
- `nan_policy=disabled`：观测 NaN 策略没有额外替换或阻断。若要主动防止 NaN 传播，应单独开启对应的 NaN 检查/保护。
- `nan_check_per_term=true`：按观测项检查数值，但不代表会自动修复 NaN。

### 3.2 Critic 观测

Critic 除了共享的本体观测，还能看到特权信息：

- `height_scan`：地形高度扫描；
- `base_lin_vel`：真实机体线速度；
- `foot_height`：足端高度；
- `foot_air_time`：足端离地时间；
- `foot_contact`：足端接触状态；
- `foot_contact_forces`：足端接触力。

本次 Critic 配置为：

- `enable_corruption=false`：Critic 不加 Actor 那样的观测损坏；
- `history_length=3`：保留最近 3 帧历史；
- `base_lin_vel` 等特权量只给 Critic，不等于真实机器人部署时 Actor 可以直接读取这些量。

这是一种非对称 Actor-Critic 结构：训练时 Critic 用更多信息估计价值，Actor 仍主要依靠可部署的本体感觉做决策。

### 3.3 动作

```yaml
actions:
  joint_pos:
    scale:
      .*_hip_joint: 0.372753
      .*_thigh_joint: 0.372753
      .*_calf_joint: 0.248502
    use_default_offset: true
```

- `joint_pos`：策略输出的是关节位置目标的增量/归一化控制量。
- `scale`：策略动作映射到各类关节的幅度。小腿动作幅度更小，减少过大的膝关节目标变化。
- `use_default_offset=true`：以机器人默认姿态为动作基准。
- `transmission_type=joint`：动作直接作用到关节传动。

动作幅度和 PD 参数会影响能否快速达到高速度；因此追踪变差时不能只看 PPO loss，还要检查扭矩、动作变化率和关节速度惩罚。

## 4. 速度命令 `commands.twist`

### 4.1 基本命令参数

| 参数 | 实际值 | 含义 |
|---|---:|---|
| `resampling_time_range` | `(5.0, 5.0)` s | 每 5 秒重新采样一次速度命令。固定相等表示没有 3--8 秒的时间抖动。 |
| `rel_standing_envs` | `0.15` | 约 15% 环境命令为站立/零速度。用于保持静止能力。 |
| `rel_heading_envs` | `0.30` | 约 30% 环境通过目标朝向生成 yaw 速度，而不是直接随机 yaw。 |
| `rel_world_envs` | `0.0` | 不使用世界坐标系速度命令，命令默认在机体坐标系。 |
| `rel_forward_envs` | `0.20` | 约 20% 环境变为纯前进命令：横向和 yaw 速度置零，x 速度取非负值。 |
| `init_velocity_prob` | `0.0` | reset 时不把机器人初始机体速度直接设置成命令速度，默认从静止状态起步。 |
| `heading_command` | `true` | 开启朝向控制。 |
| `heading_control_stiffness` | `0.5` | 朝向误差转换成 yaw 速度命令的比例系数，之后还会被 yaw 范围裁剪。 |
| `debug_vis` | `true` | 在 viewer 中显示命令相关可视化。 |

当前命令范围（S0 初始范围）是：

```text
lin_vel_x = [-1.0, 1.0] m/s
lin_vel_y = [-0.3, 0.3] m/s
ang_vel_z = [-0.5, 0.5] rad/s
heading   = [-π, π] rad
```

注意：课程会在运行时修改前三个范围，最终以 `stage` 对应的范围为准。

### 4.2 固定 x 速度桶

```text
(-1.0, -0.5, 0.10)
( 0.0,  0.0, 0.15)
( 0.5,  0.5, 0.20)
( 1.0,  1.0, 0.25)
( 1.5,  1.5, 0.15)
( 2.0,  2.0, 0.15)
```

每一项依次表示：

```text
(最小速度, 最大速度, 抽样权重)
```

由于当前每个桶是固定速度，实际就是按权重抽取离散速度。重要规则是：只有落在当前课程 `lin_vel_x` 范围内的桶才会参与抽样，剩余桶的权重会重新归一化。

因此：

- S0 的 x 范围到 `1.0`，只启用 `-1.0、0、0.5、1.0 m/s` 四个桶；`1.5` 和 `2.0 m/s` 还不会出现。
- S1 的 x 范围到 `1.5`，启用前五个桶。
- S2 的 x 范围到 `2.0`，六个桶全部启用。

S0 中可用桶的原始权重总和为 `0.70`，重新归一化后大约是：

| x 命令 | 实际抽样概率（S0） |
|---:|---:|
| `-1.0` | `14.3%` |
| `0.0` | `21.4%` |
| `0.5` | `28.6%` |
| `1.0` | `35.7%` |

这解释了为什么本次日志的高速指标为 0：`high_speed_metric_threshold=1.8`，而 S0 根本不会抽到 `1.8 m/s` 以上的命令。

### 4.3 高速和直接启动参数

| 参数 | 实际值 | 作用 |
|---|---:|---|
| `track_x_quality_std` | `0.5 m/s` | x 速度质量函数的尺度。误差越接近 0，质量越接近 1。 |
| `high_speed_metric_threshold` | `1.8 m/s` | x 命令绝对值达到该值后，样本计入高速追踪统计。 |
| `direct_start_speed_threshold` | `1.8 m/s` | reset 后，如果新命令绝对值达到该值且机器人初始速度不超过 `0.1 m/s`，记为一次直接高速启动尝试。 |
| `direct_start_window_s` | `2.0 s` | 直接启动尝试的观察窗口。当前 RL step 为 `0.02 s`，对应约 100 个策略步。 |
| `direct_start_tracking_tolerance` | `0.35 m/s` | 窗口内单步 x 速度误差不超过该值，记为一个合格步。 |
| `direct_start_success_ratio` | `0.7` | 合格步占窗口步数至少 70%，该次直接启动才记为成功。 |

直接启动指标专门回答：机器人能否从接近静止的 reset 状态，直接响应高速度命令。它不能被“先低速走起来，再切换到高速”的效果替代。

## 5. 速度课程：stage 与 phase

课程函数是 `StagedVelocityCommand`。它是全局课程：所有并行环境共享同一个 stage/phase，而不是每个环境独立升级。

### 5.1 三个 stage

| Stage | `lin_vel_x` | `lin_vel_y` | `ang_vel_z` | `randomization_scale` |
|---:|---|---|---|---:|
| S0 (`stage=0`) | `[-1.0, 1.0]` | `[-0.3, 0.3]` | `[-0.5, 0.5]` | `0.35` |
| S1 (`stage=1`) | `[-1.0, 1.5]` | `[-0.4, 0.4]` | `[-0.6, 0.6]` | `0.60` |
| S2 (`stage=2`) | `[-1.0, 2.0]` | `[-0.5, 0.5]` | `[-0.7, 0.7]` | `1.00` |

`randomization_scale` 不是始终生效的随机化系数。它只有在对应 stage 的 `phase=1` 时才会被使用；`phase=0` 时实际随机化等级固定为 0。

### 5.2 phase 的含义

每个 stage 有两个 phase：

- `phase=0`：干净学习阶段。保持当前速度范围，但关闭由课程控制的随机推力和质量/载荷随机化。
- `phase=1`：鲁棒性验证阶段。速度范围不变，开启当前 stage 的 `randomization_scale`，要求策略在扰动下仍然达标。

课程只按以下顺序推进：

```text
S0 / phase 0
        │ 达标若干评估窗口
        ▼
S0 / phase 1  ──开启 S0 的推力和质量随机化──
        │ 再次达标若干评估窗口
        ▼
S1 / phase 0  ──速度上限提高到 1.5 m/s，先关闭课程随机化──
        │
        ▼
S1 / phase 1
        │
        ▼
S2 / phase 0  ──速度上限提高到 2.0 m/s──
        │
        ▼
S2 / phase 1
```

实现上，达到 phase 0 的门槛后先切换到 phase 1；达到 phase 1 的门槛后才进入下一个 stage 的 phase 0。本实现没有自动回退到前一个 stage/phase 的逻辑，因此如果指标下降，需要人工观察并决定是否修改阈值、恢复 checkpoint 或增加训练时间。

### 5.3 课程门槛参数

| 参数 | 实际值 | 判定含义 |
|---|---:|---|
| `min_phase_episodes` | `16384` | 当前 phase 至少累计这么多 episode 结束事件后，才允许尝试升级。 |
| `evaluation_window_episodes` | `8192` | 每个评估窗口包含的 episode 数。 |
| `required_windows` | `2` | 连续两个合格窗口才升级；中间有一个不合格窗口则连续计数清零。 |
| `survival_threshold` | `0.95` | 窗口生存率至少 95%。 |
| `track_threshold` | `0.75` | 窗口总体 x 速度追踪质量至少 0.75。 |
| `high_speed_track_threshold` | `0.65` | 对已出现的高速样本，追踪质量至少 0.65。只有 stage 上限达到 `1.8 m/s` 时才启用该门槛。 |
| `direct_start_threshold` | `1.8 m/s` | stage 的最高 x 速度达到该值后，启用直接高速启动门槛。 |
| `direct_start_success_threshold` | `0.8` | 直接高速启动尝试的总体成功率至少 80%。 |

这里有两个看起来相近但用途不同的比例：

- `commands.twist.direct_start_success_ratio=0.7`：判断单次直接启动尝试是否成功；
- `curriculum.direct_start_success_threshold=0.8`：判断一批直接启动尝试是否足以让课程升级。

### 5.4 课程指标的计算方式

每个策略步的 x 速度误差为：

```text
x_error = |command_x - actual_body_velocity_x|
```

单步 x 追踪质量为：

```text
x_quality = exp(-(x_error / track_x_quality_std)^2)
```

所以：

- 误差为 0 时，`x_quality=1`；
- 误差为 `0.5 m/s` 时，质量约为 `0.368`；
- 误差为 `1.0 m/s` 时，质量约为 `0.018`。

课程的 `track_quality` 是完成 episode 后对 x 质量的平均，再在评估窗口中平均；它不是 PPO 总奖励，也不是某一个瞬时值。

`high_speed_track_quality` 只统计 `abs(command_x) >= 1.8 m/s` 的步；没有高速步时按实现输出 0，但在尚未达到高速 stage 时不应把这个 0 解释为高速能力失败。

`survival_rate` 中，一个 episode 只要活到最大 episode 长度的 95%（本次约 950 个 RL step）就算生存成功。

## 6. 随机化参数

### 6.1 由课程控制的随机化

以下两个事件读取当前课程的 `randomization_scale`：

| 事件 | 触发方式 | 本次基础范围 | 课程作用 |
|---|---|---|---|
| `push_robot` | `interval`，每 `1--5 s` | 线速度 x/y `[-0.5,0.5]`，z `[-0.4,0.4]`；roll `[-0.52,0.52]`；pitch `[-0.52,0.52]`；yaw `[-0.78,0.78]` | 所有范围乘以当前 `randomization_level`。 |
| `body_mass` | `reset` | 使用 `staged_pseudo_inertia`，保持质量和惯量物理一致 | 扰动幅度乘以当前 `randomization_level`。 |
| `base_mass_offset` | `reset` | base_link 质量加性范围 `[-1.0,2.0]` | 加性范围乘以当前 `randomization_level`。 |

因此：

- S0/phase 0、S1/phase 0、S2/phase 0：这些课程控制的推力和质量随机化实际等级为 0；
- S0/phase 1：推力和质量随机化强度为 0.35；
- S1/phase 1：强度为 0.60；
- S2/phase 1：强度为 1.00。

### 6.2 不受 phase 关闭的 startup 随机化

本次 phase 0 不是“完全无随机化”。以下事件在启动时仍然随机：

| 事件 | 范围/设置 | 作用 |
|---|---|---|
| `foot_friction_slide` | `0.3--1.5` | 足底滑动摩擦。 |
| `foot_friction_spin` | `0.0001--0.02`，log-uniform | 足底自旋摩擦。 |
| `foot_friction_roll` | `1e-5--0.005`，log-uniform | 足底滚动摩擦。 |
| `encoder_bias` | 关节偏置约 `[-0.015,0.015]` | 模拟编码器零点误差。 |
| `base_com` | x/y `±0.025`，z `±0.03` | 模拟质心偏移。 |
| `joint_damping` | 缩放 `0.9--1.1` | 模拟关节阻尼差异。 |
| `pd_gains` | kp/kd 缩放 `0.9--1.1` | 模拟控制器增益差异。 |
| `effort_limits` | 缩放 `0.9--1.1` | 模拟可用关节力矩差异。 |

这一区分很重要：如果 phase 0 的追踪质量仍然很低，主要应先解决基础速度控制和起步能力；不能简单认为“因为开了推力随机化所以学不会”，因为课程控制的推力在 phase 0 是关闭的，但摩擦、传感器和执行器随机化仍在。

## 7. 奖励参数

### 7.1 主要奖励

| 奖励 | 参数 | 权重 | 含义 |
|---|---|---:|---|
| `track_linear_velocity` | `std=0.5`，命令 `twist` | `+2.0` | x/y 线速度追踪奖励。越接近命令越高。 |
| `track_angular_velocity` | `std=0.7071` | `+2.0` | yaw 角速度追踪奖励。 |
| `upright` | `std=0.4472` | `+1.0` | 保持机身直立。 |
| `pose` | standing/walking/running 三组关节姿态标准差 | `+1.0` | 鼓励合理姿态，随运动速度切换姿态尺度。 |
| `air_time` | threshold `0.1`，command threshold `0.5` | `+0.15` | 鼓励合理摆腿/腾空时间。 |

### 7.2 惩罚项

| 项目 | 权重 | 作用 |
|---|---:|---|
| `body_ang_vel` | `-0.02` | 抑制机身角速度过大。 |
| `dof_pos_limits` | `-0.2` | 避免关节触及位置边界。 |
| `action_rate_l2` | `-0.005` | 抑制动作突变。 |
| `joint_acc_l2` | `-2.5e-7` | 抑制关节加速度。 |
| `torques_l2` | `-1e-4` | 抑制力矩消耗。 |
| `dof_vel` | `-0.001` | 抑制关节速度过大。 |
| `stance_contact` | `-1.0` | 运动时避免不合适的支撑接触。 |
| `prolonged_air_time` | `-0.5` | 避免足端长时间悬空。 |
| `foot_clearance` | `-0.2` | 使摆腿离地高度接近 `0.1 m`。 |
| `foot_swing_height` | `-0.2` | 约束摆腿高度轨迹。 |
| `foot_slip` | `-0.05` | 抑制足端接触时打滑。 |
| `soft_landing` | `-1e-5` | 轻微抑制落地冲击。 |
| `angular_momentum` | `0.0` | 当前关闭，不影响总奖励。 |

所有 reward 还受到 `scale_rewards_by_dt=true` 影响，实际按 RL step 时间进行缩放。因此不同 `decimation` 或 timestep 的实验不能只按 reward 数值直接横向比较。

### 7.3 为什么不能只看总奖励

`Train/mean_reward` 是所有奖励项加权后的总和。它可能因为站立、直立、姿态等奖励提高而上涨，即使高速 x 追踪仍然很差。

判断速度追踪时应优先看：

1. `Curriculum/command_vel/track_quality`；
2. `Metrics/twist/error_vel_x`；
3. `Episode_Reward/track_linear_velocity`；
4. 进入高速 stage 后再看 `high_speed_track_quality`；
5. 进入高速直接启动评估后再看 `direct_start_success`。

`Policy/mean_std` 是动作分布标准差，不是速度误差标准差。它下降通常说明探索收敛，但不能单独证明速度追踪变好。

## 8. PPO 参数

### 8.1 Rollout 和训练规模

| 参数 | 实际值 | 含义 |
|---|---:|---|
| `num_steps_per_env` | `24` | 每个环境每个 iteration 收集 24 个策略步。 |
| `num_envs` | `4096` | 并行环境数。每轮 rollout 约有 `4096×24=98304` 个环境步。 |
| `max_iterations` | `15000` | 最大优化轮数。 |
| `save_interval` | `200` | checkpoint 保存周期。 |
| `resume` | `false` | 是否从旧 run 恢复。 |
| `load_run` | `.*` | 恢复时用于匹配 run 名称的模式。 |
| `load_checkpoint` | `model_.*.pt` | 恢复时匹配 checkpoint 文件的模式。 |

### 8.2 网络结构

Actor 和 Critic 都是：

```text
MLP: 512 → 256 → 128
activation: ELU
obs_normalization: false
```

- `hidden_dims`：每层隐藏层宽度。网络较大，拟合能力强，但更新也可能更敏感、显存占用更高。
- `activation=elu`：激活函数。
- `obs_normalization=false`：不额外做观测归一化；当前主要依赖各观测本身的尺度和噪声设置。
- Actor `init_std=1.0`：动作高斯分布初始标准差。
- Actor `std_type=scalar`：动作维度共享一个可学习的标量标准差。
- Critic 没有动作分布配置，因为它输出价值估计。

### 8.3 PPO 更新参数

| 参数 | 实际值 | 含义 |
|---|---:|---|
| `num_learning_epochs` | `5` | 每批 rollout 数据重复优化 5 遍。 |
| `num_mini_batches` | `4` | 每遍分成 4 个 mini-batch。 |
| `learning_rate` | `1e-5` | 初始学习率。当前为较小值，收敛慢但更新更保守。 |
| `schedule` | `adaptive` | 根据实际 KL 相对目标 KL 自适应调整学习率。 |
| `desired_kl` | `0.01` | 自适应学习率希望维持的 KL 量级。 |
| `gamma` | `0.99` | 未来奖励折扣因子。 |
| `lam` | `0.95` | GAE 的 bias/variance 折中参数。 |
| `entropy_coef` | `0.01` | 熵奖励系数，鼓励探索。过大可能不收敛，过小可能过早确定。 |
| `clip_param` | `0.2` | PPO 策略概率比裁剪范围。 |
| `use_clipped_value_loss` | `true` | 对 Critic 的价值更新也做裁剪。 |
| `value_loss_coef` | `1.0` | value loss 在总损失中的权重。 |
| `max_grad_norm` | `1.0` | 梯度裁剪上限，降低梯度爆炸风险。 |
| `optimizer` | `adam` | 优化器。 |
| `normalize_advantage_per_mini_batch` | `false` | 不在每个 mini-batch 内单独归一化 advantage。 |

重点是：`Loss/value`、`Loss/surrogate` 和 `Loss/entropy` 只能说明优化过程，不能替代速度追踪指标。尤其 `Loss/surrogate` 为负值是 PPO 目标的常见表现，不等于训练失败。

## 9. TensorBoard 指标逐项说明

### 9.1 课程指标

| TensorBoard tag | 范围/单位 | 解释 |
|---|---|---|
| `Curriculum/command_vel/stage` | `0/1/2` | 当前速度 stage，分别对应 S0/S1/S2。 |
| `Curriculum/command_vel/phase` | `0/1` | 当前 stage 的干净学习/随机化验证 phase。 |
| `Curriculum/command_vel/randomization_level` | `0, 0.35, 0.60, 1.0` | 当前实际用于课程推力和质量随机化的等级。 |
| `Curriculum/command_vel/survival_rate` | `0--1` | 当前评估窗口的生存率。 |
| `Curriculum/command_vel/track_quality` | `0--1` | 当前评估窗口的总体 x 速度质量。 |
| `Curriculum/command_vel/high_speed_track_quality` | `0--1` | `abs(command_x)>=1.8` 的样本质量。没有高速样本时为 0。 |
| `Curriculum/command_vel/direct_start_success` | `0--1` | 直接高速启动尝试的成功率。没有尝试时为 0。 |

### 9.2 速度误差和累计量

| TensorBoard tag | 解释 |
|---|---|
| `Metrics/twist/error_vel_xy` | x/y 平面线速度绝对误差的 episode 平均，单位 m/s。 |
| `Metrics/twist/error_vel_x` | x 速度绝对误差的 episode 平均，单位 m/s；判断前进速度最重要的误差之一。 |
| `Metrics/twist/error_vel_y` | y 速度绝对误差的 episode 平均，单位 m/s。 |
| `Metrics/twist/error_vel_yaw` | yaw 角速度绝对误差的 episode 平均，单位 rad/s。 |
| `Metrics/twist/track_x_quality_sum` | 当前 episode 累积的 x 质量和，不应直接当作质量。 |
| `Metrics/twist/track_x_step_count` | 当前 episode 累计参与 x 质量计算的步数。 |
| `Metrics/twist/high_speed_track_sum` | 高速步的 x 质量累积。 |
| `Metrics/twist/high_speed_step_count` | 高速步数量。 |
| `Metrics/twist/direct_start_attempt` | 当前 episode 是否发生过直接高速启动尝试。 |
| `Metrics/twist/direct_start_success` | 当前 episode 的直接启动结果，1 成功、0 失败。 |

如果要由累计量手工复核：

```text
track_quality_episode ≈ track_x_quality_sum / track_x_step_count
high_speed_quality ≈ high_speed_track_sum / high_speed_step_count
```

分母为 0 时不能解释为失败，只能解释为该 episode 没有对应样本。

### 9.3 Episode reward 与终止

常用 tag：

- `Episode_Reward/track_linear_velocity`：线速度追踪奖励，越高通常越好；
- `Episode_Reward/track_angular_velocity`：角速度追踪奖励；
- `Episode_Reward/upright`：直立奖励；
- `Episode_Reward/pose`：姿态奖励；
- `Episode_Reward/stance_contact`、`foot_slip`、`torques_l2`、`action_rate_l2`、`joint_acc_l2`、`dof_vel`：对应接触、打滑、力矩、动作变化、加速度和关节速度项；
- `Episode_Termination/time_out`：正常跑满 episode；
- `Episode_Termination/fell_over`：姿态超过 `1.22173 rad`（约 70°）而结束；
- `Episode_Termination/illegal_contact`：大腿接触地面而结束。

### 9.4 训练过程指标

| TensorBoard tag | 解释 |
|---|---|
| `Train/mean_reward` | 所有 reward 项加权后的总回报。用于看整体趋势，不是单独的速度指标。 |
| `Train/mean_episode_length` | 平均 episode 长度，接近 1000 表示大多数环境能活满 20 秒。 |
| `Loss/value` | Critic 价值损失。 |
| `Loss/surrogate` | PPO 策略 surrogate loss。 |
| `Loss/entropy` | 策略熵相关量，通常随探索减弱而下降。 |
| `Loss/learning_rate` | adaptive schedule 当前实际学习率，可能与 YAML 初始值不同。 |
| `Policy/mean_std` | Actor 动作分布标准差，不是速度追踪误差。 |

## 10. 如何从日志判断当前阶段

优先直接读取下面三个 tag 的最新值：

```text
Curriculum/command_vel/stage
Curriculum/command_vel/phase
Curriculum/command_vel/randomization_level
```

判定表：

| `stage` | `phase` | 速度范围 | 课程推力/质量随机化 |
|---:|---:|---|---|
| `0` | `0` | x 到 `1.0 m/s` | 关闭，level `0` |
| `0` | `1` | x 到 `1.0 m/s` | S0 强度 `0.35` |
| `1` | `0` | x 到 `1.5 m/s` | 关闭，level `0` |
| `1` | `1` | x 到 `1.5 m/s` | S1 强度 `0.60` |
| `2` | `0` | x 到 `2.0 m/s` | 关闭，level `0` |
| `2` | `1` | x 到 `2.0 m/s` | S2 强度 `1.00` |

不能只通过 reward 猜 stage。原因是 stage 是课程管理器的状态，而总 reward 可能在同一个 stage 内继续上涨或因随机化产生波动。

## 11. 如何判断是否满足升级条件

当前窗口需要同时满足：

```text
survival_rate             >= 0.95
track_quality             >= 0.75
若当前 stage 上限 >= 1.8 m/s：
    high_speed_track_quality >= 0.65
    direct_start_success     >= 0.80，且必须存在 direct-start 尝试
```

并且还要满足：

```text
当前 phase 至少累计 16384 个 episode
当前评估窗口至少累计 8192 个 episode
连续 2 个评估窗口都合格
```

升级过程是：

- phase 0 合格两次：进入同 stage 的 phase 1，开始课程推力/质量随机化；
- phase 1 合格两次：进入下一个 stage 的 phase 0，速度上限提高，同时暂时关闭课程随机化；
- S2/phase 1 再合格：课程达到末阶段，不再有更高 stage。

如果 `stage` 和 `phase` 长时间不变，先检查对应指标是否达到阈值，再检查 episode 计数是否已经超过 `min_phase_episodes` 和 `evaluation_window_episodes`。指标达到但状态没变，常见原因是还没有完成两个连续合格窗口。

## 12. 如何判断速度追踪效果

建议按以下顺序看，而不是只看一条曲线。

### 12.1 基础生存能力

先看：

```text
Train/mean_episode_length
Episode_Termination/fell_over
Episode_Termination/illegal_contact
Curriculum/command_vel/survival_rate
```

经验判断：

- `mean_episode_length` 接近 1000 且 `survival_rate >= 0.95`：稳定性基本达到课程要求；
- 生存率高但 `track_quality` 低：机器人可能只是站住了，没有有效跟踪命令；
- 生存率低：此时不能通过调高速度课程阈值来解决，应先看摔倒、非法接触和动作/力矩是否异常。

### 12.2 低速和当前速度范围的追踪

看：

```text
Curriculum/command_vel/track_quality
Metrics/twist/error_vel_x
Episode_Reward/track_linear_velocity
```

其中：

- `track_quality` 越接近 1 越好，课程门槛是 0.75；
- `error_vel_x` 越低越好，但它是 m/s 误差，不能和 0--1 的 `track_quality` 直接比较；
- `Episode_Reward/track_linear_velocity` 受 `weight=2.0` 和时间缩放影响，只适合观察趋势和横向对比相同配置的实验。

### 12.3 高速追踪

只有 `stage=1` 的上限为 `1.5 m/s` 时仍不会产生 `1.8 m/s` 高速统计；要到 `stage=2` 才会有 `2.0 m/s` 桶，`high_speed_track_quality` 才有足够意义。

进入高速 stage 后，应同时确认：

- `Metrics/twist/high_speed_step_count` 持续大于 0；
- `high_speed_track_quality >= 0.65`；
- `error_vel_x` 没有随着 stage 提高而持续恶化；
- `direct_start_attempt` 不为 0；
- `direct_start_success >= 0.80`。

如果高速样本数为 0，`high_speed_track_quality=0` 只是“没有统计对象”，不能当作高速追踪失败。

### 12.4 直接从静止启动

在 viewer 中测试“直接把命令设为 2 m/s”时，应重点关注：

```text
direct_start_attempt
direct_start_success
direct_start_success_ratio = 0.7
direct_start_window_s = 2.0
direct_start_tracking_tolerance = 0.35
```

这项测试和“从 1 m/s 切换到 2 m/s”不同：后者可以利用已经建立的步态动量，不能代表 reset 后直接启动能力。

## 13. 本次日志的实际结论

根据 TensorBoard 最后一个点（step 377）：

| 指标 | 首值 | 末值 | 课程要求/解释 |
|---|---:|---:|---|
| `stage` | `0` | `0` | 仍在 S0。 |
| `phase` | `0` | `0` | 仍在干净学习阶段。 |
| `randomization_level` | `0` | `0` | 课程控制的推力和质量随机化尚未开启。 |
| `survival_rate` | `1.000` | `0.978` | 末值高于 `0.95`，生存基本达标。 |
| `track_quality` | `0.447` | `0.441` | 明显低于 `0.75`，速度追踪尚未达标。 |
| `high_speed_track_quality` | `0` | `0` | 当前 S0 没有 `>=1.8 m/s` 样本，不能评价高速。 |
| `direct_start_success` | `0` | `0` | 当前没有直接高速启动尝试，不能评价直接启动。 |
| `error_vel_x` | `0.028` | `2.127` | 末期 x 误差较大，需结合命令分布和 episode 统计继续排查。 |
| `track_linear_velocity` | `0.007` | `0.747` | 有提升，但仍不能据此证明已达到课程追踪门槛。 |
| `mean_episode_length` | `13.3` | `979.4` | 从频繁早摔进步到接近跑满。 |
| `mean_reward` | `0.354` | `67.121` | 总奖励上涨，主要说明整体行为变好，不等于高速追踪合格。 |
| `Policy/mean_std` | `0.998` | `0.484` | 探索幅度下降，动作分布更集中。 |

结论：

1. 当前课程没有卡在“随机推力太强”上，因为 `randomization_level=0`。
2. 当前首先卡在 `track_quality`，不是生存率。机器人已经大体能活下来，但速度命令跟踪质量不足。
3. 目前还没有进入 1.8--2.0 m/s 高速评估，因此不能从本次日志判断 2 m/s 直接启动是否合格。
4. 这次 run 只有很早期的训练数据，建议继续训练观察 `track_quality` 是否向 `0.75` 上升；如果长时间横盘，应进一步检查命令采样、实际速度、动作输出和 `error_vel_x` 的统计方式。

## 14. 建议的 TensorBoard 判读流程

启动 TensorBoard 后，先固定查看以下曲线：

```bash
tensorboard --logdir /home/haozi/桌面/mjlab/logs/rsl_rl/go2_flat/2026-08-15_09-09-24
```

建议按这个顺序：

1. 查看 `stage/phase/randomization_level`，确定正在解决什么问题。
2. 查看 `mean_episode_length`、`survival_rate` 和摔倒/非法接触，判断是否站得住。
3. 查看 `track_quality` 和 `error_vel_x`，判断当前速度是否跟得上。
4. 进入 S2 后确认 `high_speed_step_count > 0`，再评价高速质量。
5. 确认存在 `direct_start_attempt` 后，再评价 `direct_start_success`。
6. 最后查看 `mean_reward`、`track_linear_velocity`、`upright` 和各惩罚项，分析整体行为为什么变化。

最简单的自动判定逻辑可以写成：

```text
if stage == 0 and phase == 0:
    目标 = survival_rate >= 0.95 且 track_quality >= 0.75
elif phase == 1:
    目标 = survival_rate >= 0.95 且 track_quality >= 0.75
    如果 stage 上限 >= 1.8:
        还要 high_speed_track_quality >= 0.65
        还要 direct_start_success >= 0.80
```

无论哪种 phase，都必须检查该结果是否在两个连续评估窗口中成立，不能用单个尖峰判断达标。

## 15. 常见误判和排查方向

### 15.1 总奖励上升，但速度仍然跟不上

可能是 `upright`、`pose` 或站立相关奖励先变好。检查 `track_quality`、`error_vel_x` 和 `track_linear_velocity`，不要只看 `mean_reward`。

### 15.2 `high_speed_track_quality=0`

先看 `high_speed_step_count`。如果为 0，说明还没有高速命令，不是高速策略失败。

### 15.3 `direct_start_success=0`

先看 `direct_start_attempt`。没有尝试时该值按实现默认为 0；只有真正抽到高速度命令并从接近静止 reset，才会启动计时窗口。

### 15.4 生存率达标但课程不升级

还需要 `track_quality` 达标，并且满足最小 episode 数、评估窗口数量和连续窗口数。高速 stage 还需要额外的高速和直接启动门槛。

### 15.5 `Policy/mean_std` 下降就认为追踪变好

它只是动作分布的标准差。应同时看到 `track_quality` 上升、`error_vel_x` 下降，才说明速度控制真的改善。

### 15.6 日志出现 NaN 但没有快照

本次 `sim.nan_guard.enabled=false`，Actor/Critic 的 `nan_policy` 也为 `disabled`。如果要定位 NaN，需要开启 NaN guard 或在训练/观测/奖励处增加数值检查，并同时查看 `Loss/*`、reward 和实际状态是否先出现 Inf。

## 16. 一页式判断标准

可以把当前训练归类为以下四种状态：

| 状态 | 典型表现 | 处理建议 |
|---|---|---|
| 尚未学会站稳 | `survival_rate<0.95`，episode length 短，摔倒多 | 先解决姿态、接触和基础步态。 |
| 站稳但不会跟速 | 生存率高，`track_quality<0.75`，`error_vel_x` 大 | 继续训练或检查命令/动作/奖励，暂不进入下一课程。 |
| 当前速度已稳定 | 生存率和 `track_quality` 均达标，连续窗口满足 | 等待课程自动进入 phase 1 或下一 stage，并观察随机化后的指标。 |
| 高速鲁棒性已验证 | S2、phase 1，high-speed 和 direct-start 指标均达标 | 才能认为 2 m/s 直接启动和扰动下速度追踪基本通过。 |

对本次日志的归类是：**“站稳能力接近达标，但当前 S0 速度追踪尚未达标；仍处于 phase 0，暂未开始课程控制的随机推力。”**
